/**
 * Smart Importer service: single entry point for parsing a recipe from a URL.
 * Returns { title, ingredients[], steps[], servings? }.
 * Placeholder: uses existing parser API; can be replaced with AI parsing later.
 */
export { parseRecipeFromUrl } from "@/lib/recipeParser";
